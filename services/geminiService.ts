
// Service removed to prevent runtime initialization errors.
// All AI content generation is now handled via static content in the UI.
export const generateMarketingCopy = async (topic: string): Promise<string> => {
  return "";
};
